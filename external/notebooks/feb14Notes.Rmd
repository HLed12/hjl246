set.seed(50)
dat <- data.frame(v1 = 1:20, v2 = sample(1:100, 20, replace = TRUE),
                  v3 = runif(20, 0, 50), v4 = sample(LETTERS, 20, replace = TRUE))
                  
                  
dat

# can't index v4 based on v2 because v2 isn't created outside of the function!

# assignment 2 is due friday
# exercises from 2/14 class slades and unit 1 module 4 
# send code snippet with 3 back-ticks to Vanchy for the answer to exercises (took photo on phone)
