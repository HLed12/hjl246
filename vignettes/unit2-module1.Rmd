---
title: "unit2-module1"
author: "Harrison Leduc"
date: "2024-03-11"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{unit2-module1}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(hjl246)
```

## Task 1

```{r}
roads <- st_read(system.file("extdata/roads.geojson", package = "geospaar"))
roads

districts_alb <- read_sf(system.file("extdata/districts.geojson", 
                                 package = "geospaar")) %>% 
  st_transform(x = ., crs = st_crs(roads))
districts_alb

farmers_alb <- read_csv(system.file("extdata/farmer_spatial.csv", package = "geospaar")) %>% 
  distinct(uuid, x, y, season) %>% 
  st_as_sf(x = ., coords = c("x", "y"), crs = 4326) %>%
  st_transform(x = ., crs = st_crs(roads))
farmers_alb

```

## Task 2

```{r, fig.width=6, fig.height=4, fig.align = "center"}
library(sf)
#par(mar = c(0, 0, 0, 0))

plot(districts_alb %>% st_geometry(), col = "grey", graticule = TRUE, axes = TRUE)
plot(roads, add = TRUE, col = "red", lwd = 2) # or pch = 20
plot(farmers_alb, add = TRUE, pch = "+", size = 3, col = "blue")

```

## Task 3

```{r, fig.width=5, fig.height=6, fig.align = "center"}
ggplot() +
  geom_sf(data = districts_alb, fill = "grey") +
  geom_sf(data = roads, col = "red") +
  geom_sf(data = farmers_alb, pch = "+", size = 3, col = "blue") +
  theme_bw()

```


## Task 4

```{r, fig.width=6, fig.height=4, fig.align = "center"}
# use par(mar(0?,0?,1?,0?)) ?? to get the 1 inch marg at top
median_dist <- districts_alb %>% 
  mutate(area = st_area(.) %>% 
  units::set_units("km^2") %>%
  as.numeric()) %>% 
  summarize(median(area))
median_dist

median_dist <- districts_alb %>% 
  mutate(area = as.numeric(st_area(.))) %>% 
  summarize(median(area))
median_dist

districts_alb %>% 
  mutate(distName = distName, geometry = geometry, area = as.numeric(st_area(.))) %>% 
  summarize(median(.$data))


districts_alb
plot(districts_alb %>% st_geometry(), col = "grey", graticule = TRUE, axes = TRUE, main = "The median area district")
plot(median_dist, col = "khaki") # or pch = 20

ggplot() + 
  geom_sf(data = districts_alb, fill = "grey") +
  geom_sf(data = median_dist, col = "khaki")

ggplot(districts_alb, fill = "grey") +
  geom_sf(data = median_dist, col = "khaki")

districts_alb %>% plot(col = "grey", reset = FALSE)
median_dist %>% plot(col = "khaki", add = TRUE)

test <- districts_alb %>% 
  mutate(area = st_area(.) %>% 
  units::set_units("km^2") %>%
  as.numeric())
test
test$area
median(test$area)
test$area == 9441.404
test1 <- districts_alb %>% 
  mutate(area = st_area(.) %>% 
  units::set_units("km^2") %>%
  as.numeric()) %>% 
  select(.$area== median(.$area)) #match(median(.$area))
test1

test2 <- districts_alb %>% 
  mutate(area = st_area(.) %>% 
  units::set_units("km^2") %>%
  as.numeric()) %>% 
  summarize(median(area)) #match(median(.$area))
test2
```

```{r}

districts_alb %>% 
  mutate(distName = distName, geometry = geometry, area = as.numeric(st_area(.))) %>% 
  summarize(median(.$data))

dist_test <- districts_alb %>% 
  mutate(distName = distName, geometry = geometry, area = as.numeric(st_area(.))) %>% 
  summarize(median(.$area))
dist_test

distTest <- districts_alb
distTest
# distTest <- mutate(distName = distName, geometry = geometry, area = as.numeric(st_area(.))) 
distTest$area = as.numeric(st_area(districts_alb))
distTest

median(distTest$area)
distTest[1,1:3]


median_test2 <- distTest[abs(distTest$area - median_area) <= tolerance, ]
median_test2

distTest[distTest$area == median_area, ]

plot(distTest %>% st_geometry(), col = "grey", graticule = TRUE, axes = TRUE, main = "The median area district")
plot(median_test2, col = "khaki", add = TRUE)  # Add the median district to the existing plot

```
